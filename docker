Primero ejecutar esto en la terminal

docker run -d \
  --name mysql-server \
  -e MYSQL_ROOT_PASSWORD=root123 \
  -e MYSQL_DATABASE=aplicaciones_web \
  -e MYSQL_USER=estudiante \
  -e MYSQL_PASSWORD=estudiante123 \
  -p 3307:3306 \
  mysql:8.0

jdbc:mysql://localhost:3307/aplicaciones_web?allowPublicKeyRetrieval=true&useSSL=false


CREATE TABLE productos (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(255) NOT NULL,
    descripcion TEXT NOT NULL,
    precio VARCHAR(50) NOT NULL
);

docker run -d \
  -p 83:80 \
  -v "/Users/nayelicorona/Documents/UTN/cuatrimestre5/aplicacionesWeb/aplicaciones-web/php/html:/var/www/html" \
  --name servidor-php83 \
  php:8.1-apache \
  bash -c "docker-php-ext-install mysqli && apache2-foreground"